# 🎨 Visual Architecture & Flow Diagrams

Complete visual guide to understanding the system.

---

## 🔄 Authentication Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    APPLICATION LAUNCH                            │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ↓
            ┌──────────────────────────────┐
            │  Check Supabase Auth State   │
            │  currentUser == null?        │
            └──────┬──────────────────┬────┘
                   │ YES              │ NO
                   ↓                  ↓
        ┌─────────────────────┐   ┌──────────────────┐
        │  LoginScreen        │   │  HomePage        │
        │  "Sign in w/ Google"│   │  (Show Tasks)    │
        │  Button             │   │                  │
        └────────┬────────────┘   └──────────────────┘
                 │
        ┌────────┴────────────────────────┐
        │ User Clicks "Sign in w/ Google" │
        └────────┬─────────────────────────┘
                 │
                 ↓
        ┌─────────────────────────────┐
        │  Google Login Popup         │
        │  (User enters credentials)  │
        └────────┬────────────────────┘
                 │
                 ↓
        ┌─────────────────────────────┐
        │  Google Confirms Auth       │
        │  Returns: idToken, accessToken
        └────────┬────────────────────┘
                 │
                 ↓
        ┌────────────────────────────────┐
        │  Supabase signInWithIdToken()  │
        └────────┬─────────────────────┘
                 │
                 ↓
        ┌──────────────────────────┐
        │  Save User to DB         │
        │  users table:            │
        │  - id (from auth)        │
        │  - email (from Google)   │
        │  - full_name (from Google)
        │  - profile_image_url     │
        │  - auth_provider: 'google'
        └────────┬─────────────────┘
                 │
                 ↓
        ┌──────────────────────────┐
        │  Navigate to HomePage    │
        │  Load user's tasks       │
        └──────────────────────────┘
```

---

## 📊 Data Flow - Creating a Task

```
User Interface              Service Layer           Database Layer
┌──────────────┐         ┌──────────────┐        ┌──────────────────┐
│ Task Form    │         │  SupabaseServ│        │  Supabase        │
│ - Title      │         │  ice         │        │  (PostgreSQL)    │
│ - Descripton │         │              │        │                  │
│ - Priority   │──────→  │ createTask() │───────→│  INSERT INTO     │
│ - Category   │         │  {           │        │  tasks (         │
│ - Due Date   │         │   title,     │        │    id,           │
│              │         │   priority,  │        │    user_id,      │
│ [Create]     │         │   category   │        │    title,        │
└──────────────┘         │  }           │        │    priority,     │
                         │              │        │    category,     │
                         └──────┬───────┘        │    created_at    │
                                │                │  )               │
                                │                │                  │
                                │  Response      └──────────────────┘
                                │  ← ← ← ← ← ← ← ↓
                                │   Task object  │
                                │   with ID      │
                         ┌──────▼───────┐
                         │  Update UI   │
                         │  - Add task  │
                         │    to list   │
                         │  - Show      │
                         │    success   │
                         └──────────────┘
```

---

## 🔒 Row Level Security (RLS) - How It Works

```
WITHOUT RLS (Unsafe):
┌──────────────────┐
│  User A Login    │
│  Can query:      │
│  SELECT * FROM   │
│  tasks;          │
│                  │
│  Returns ALL     │
│  tasks (User B's │
│  data included)  │ ❌ SECURITY RISK
│                  │
└──────────────────┘

WITH RLS (Safe):
┌──────────────────────────────────┐
│  User A Login (ID: abc123)       │
│  Can query:                      │
│  SELECT * FROM tasks;            │
│                                  │
│  Database automatically adds:    │
│  WHERE user_id = 'abc123'        │
│                                  │
│  Returns ONLY User A's tasks ✅  │
│  User B's data is hidden         │
└──────────────────────────────────┘

Even if hacker tries:
┌──────────────────────────────────┐
│  User A (Hacker) sends:          │
│  SELECT * FROM tasks             │
│  WHERE user_id = 'xyz789'        │
│  (Trying to get User B's data)   │
│                                  │
│  Supabase blocks! ❌             │
│  RLS policy prevents access      │
│  Hacker gets permission denied   │
└──────────────────────────────────┘
```

---

## 📱 App Architecture - MVVM Pattern

```
┌─────────────────────────────────────────────────────────────────┐
│                        PRESENTATION LAYER                        │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌─────────────────┐  │
│  │ Login    │  │ Task     │  │ Task     │  │ Settings       │  │
│  │ Screen   │  │ Form     │  │ List     │  │ Screen        │  │
│  │          │  │ Screen   │  │ Screen   │  │                │  │
│  └─────┬────┘  └────┬─────┘  └────┬─────┘  └────┬───────────┘  │
│        │             │             │             │              │
└────────┼─────────────┼─────────────┼─────────────┼──────────────┘
         │             │             │             │
         │             ↓             ↓             ↓
┌────────┼─────────────────────────────────────────────────────┐
│         │            VIEWMODEL LAYER (Provider)              │
│         │  ┌──────────────────┐  ┌──────────────────┐       │
│         └→ │ UserViewModel    │  │ TaskViewModel    │       │
│            │ - user: User?    │  │ - tasks: List    │       │
│            │ - isLoading      │  │ - isLoading      │       │
│            │ - updateSettings │  │ - createTask()   │       │
│            │ - loadProfile()  │  │ - fetchTasks()   │       │
│            │                  │  │ - deleteTask()   │       │
│            └────────┬─────────┘  └────────┬────────┘       │
│                     │                     │                │
└─────────────────────┼─────────────────────┼─────────────────┘
                      │                     │
         ┌────────────┴─────────────────────┴────────────┐
         │                                              │
         ↓                                              ↓
┌────────────────────────────────────────────────────────────┐
│              SERVICE LAYER (SupabaseService)              │
│  ┌───────────────┐  ┌──────────────┐  ┌─────────────────┐ │
│  │ Authentication│  │ Task Ops     │  │ User Ops       │ │
│  │ - signInWith  │  │ - createTask │  │ - getUserProf. │ │
│  │   Google()    │  │ - fetchTasks │  │ - updateSett.  │ │
│  │ - signOut()   │  │ - updateTask │  │                │ │
│  │ - getCurrentU │  │ - deleteTask │  │                │ │
│  │   serId()     │  │              │  │                │ │
│  └───────────────┘  └──────────────┘  └─────────────────┘ │
└────────────────────────────────────────────────────────────┘
         │
         ↓
┌────────────────────────────────────────────────────────────┐
│         DATA LAYER (Supabase Cloud Database)              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌───────────┐  │
│  │ users    │  │ tasks    │  │ categories│ │ subtasks  │  │
│  │ table    │  │ table    │  │ table    │  │ table    │  │
│  │          │  │          │  │          │  │          │  │
│  │ id       │  │ id       │  │ id       │  │ id       │  │
│  │ email    │  │ user_id  │  │ user_id  │  │ task_id  │  │
│  │ full_name│  │ title    │  │ name     │  │ title    │  │
│  │ auth_prov│  │ priority │  │ color    │  │ done     │  │
│  │          │  │ category │  │ icon     │  │          │  │
│  │ RLS: ✅  │  │ RLS: ✅  │  │ RLS: ✅  │  │ RLS: ✅  │  │
│  └──────────┘  └──────────┘  └──────────┘  └───────────┘  │
└────────────────────────────────────────────────────────────┘
```

---

## 🔄 Task Lifecycle

```
┌─────────────────────────────────────────────────────────────┐
│                     TASK LIFECYCLE                          │
└─────────────────────────────────────────────────────────────┘

1. CREATE
   ┌─────────────────────────────┐
   │ User fills form             │
   │ - Title: "Buy groceries"    │
   │ - Priority: High            │
   │ - Category: Shopping        │
   │ - Due: Tomorrow             │
   └────────┬────────────────────┘
            │
            ↓
   ┌─────────────────────────────┐
   │ SupabaseService.createTask()│
   │ Inserts into tasks table    │
   │ Sets: user_id = current     │
   │       created_at = now()    │
   └────────┬────────────────────┘
            │
            ↓
   ┌─────────────────────────────┐
   │ Task appears in DB ✅       │
   │ Task shows in UI            │
   └─────────────────────────────┘

2. EDIT
   ┌─────────────────────────────┐
   │ User taps task              │
   │ Can edit:                   │
   │ - Title                     │
   │ - Description               │
   │ - Priority                  │
   │ - Due date                  │
   └────────┬────────────────────┘
            │
            ↓
   ┌─────────────────────────────┐
   │ SupabaseService.updateTask()│
   │ Updates row in DB           │
   │ Sets: updated_at = now()    │
   └────────┬────────────────────┘
            │
            ↓
   ┌─────────────────────────────┐
   │ Changes saved ✅            │
   └─────────────────────────────┘

3. COMPLETE
   ┌─────────────────────────────┐
   │ User checks checkbox        │
   └────────┬────────────────────┘
            │
            ↓
   ┌─────────────────────────────┐
   │ toggleTaskCompletion(id)    │
   │ Sets: is_completed = true   │
   │       updated_at = now()    │
   └────────┬────────────────────┘
            │
            ↓
   ┌─────────────────────────────┐
   │ Task marked as done ✅      │
   │ Shows strikethrough in UI   │
   └─────────────────────────────┘

4. DELETE
   ┌─────────────────────────────┐
   │ User taps delete button     │
   └────────┬────────────────────┘
            │
            ↓
   ┌─────────────────────────────┐
   │ SupabaseService.deleteTask()│
   │ Deletes from tasks table    │
   │ Also deletes:               │
   │ - Associated subtasks       │
   │ - Associated tags           │
   │ - Associated attachments    │
   │ (CASCADE delete)            │
   └────────┬────────────────────┘
            │
            ↓
   ┌─────────────────────────────┐
   │ Task removed ✅             │
   │ Removed from UI             │
   │ Removed from DB             │
   └─────────────────────────────┘
```

---

## 🌊 State Management Flow (Provider)

```
┌─────────────────────────────────────────────┐
│  User Interacts with UI                     │
│  (Clicks button, submits form, etc)         │
└────────┬────────────────────────────────────┘
         │
         ↓
┌─────────────────────────────────────────────┐
│  ViewModel Method Called                    │
│  Example: TaskViewModel.createTask(...)     │
└────────┬────────────────────────────────────┘
         │
         ↓
┌─────────────────────────────────────────────┐
│  Call SupabaseService                       │
│  Example: SupabaseService().createTask(...) │
└────────┬────────────────────────────────────┘
         │
         ↓
┌─────────────────────────────────────────────┐
│  Database Operation (Async)                 │
│  Data sent to Supabase                      │
│  Data saved in PostgreSQL                   │
└────────┬────────────────────────────────────┘
         │
         ↓
┌─────────────────────────────────────────────┐
│  Get Response                               │
│  Task object or error                       │
└────────┬────────────────────────────────────┘
         │
         ↓
┌─────────────────────────────────────────────┐
│  Update ViewModel State                     │
│  Example: _tasks.add(newTask)               │
└────────┬────────────────────────────────────┘
         │
         ↓
┌─────────────────────────────────────────────┐
│  Call notifyListeners()                     │
│  Provider detects change                    │
└────────┬────────────────────────────────────┘
         │
         ↓
┌─────────────────────────────────────────────┐
│  UI Rebuilds Automatically                  │
│  Consumer<TaskViewModel> rebuilds           │
│  Displays new task in list                  │
└─────────────────────────────────────────────┘
```

---

## 📍 URL/Endpoint Structure

```
Supabase Project:
URL: https://[PROJECT-ID].supabase.co
API Key: [ANON_KEY]

Database Tables:
- /rest/v1/users
- /rest/v1/tasks
- /rest/v1/categories
- /rest/v1/subtasks
- /rest/v1/task_tags
- /rest/v1/task_attachments

Authentication:
- /auth/v1/signup
- /auth/v1/token?grant_type=password
- /auth/v1/logout
```

---

## 🔐 Security Layers

```
┌──────────────────────────────────────────┐
│  LAYER 1: Frontend Authentication       │
│  ┌────────────────────────────────────┐  │
│  │ Check if currentUser exists        │  │
│  │ Guard routes (AuthWrapper)         │  │
│  │ Show/hide UI based on auth state   │  │
│  └────────────────────────────────────┘  │
└──────────────────────────────────────────┘
         │
         ↓
┌──────────────────────────────────────────┐
│  LAYER 2: API Authentication            │
│  ┌────────────────────────────────────┐  │
│  │ Every request includes JWT token   │  │
│  │ Token from Supabase Auth           │  │
│  │ Supabase validates token           │  │
│  └────────────────────────────────────┘  │
└──────────────────────────────────────────┘
         │
         ↓
┌──────────────────────────────────────────┐
│  LAYER 3: Row Level Security (RLS)      │
│  ┌────────────────────────────────────┐  │
│  │ Every query filtered by user_id    │  │
│  │ Can't access other users' data     │  │
│  │ Enforced at database level         │  │
│  │ Even if API compromised, data safe │  │
│  └────────────────────────────────────┘  │
└──────────────────────────────────────────┘
         │
         ↓
┌──────────────────────────────────────────┐
│  LAYER 4: Data Integrity                │
│  ┌────────────────────────────────────┐  │
│  │ Foreign keys prevent orphaned data │  │
│  │ Cascading deletes clean up         │  │
│  │ Constraints validate data types    │  │
│  │ Indexes optimize queries           │  │
│  └────────────────────────────────────┘  │
└──────────────────────────────────────────┘
```

---

## 📊 Database Relationships

```
User (1) ────────────→ (Many) Tasks
  │
  ├─ Has one profile
  ├─ Can have many tasks
  ├─ Can have many categories
  └─ Tasks can have many subtasks

Task (1) ────────────→ (Many) Subtasks
Task (1) ────────────→ (Many) Tags
Task (1) ────────────→ (Many) Attachments

Category (1) ────────→ (Many) Tasks
User (1) ────────────→ (Many) Categories

Example:
┌─────────────┐
│   User 1    │
├─────────────┤
│ john@example│
│ John Doe    │
└────┬────────┘
     │
     ├→ Category: "Work"
     │  ├→ Task: "Report due"
     │  ├→ Task: "Meeting"
     │  └→ Task: "Code review"
     │
     ├→ Category: "Personal"
     │  ├→ Task: "Buy milk"
     │  │  └→ Subtask: "Check expiry"
     │  ├→ Task: "Exercise"
     │  └→ Task: "Read book"
     │
     └→ Category: "Shopping"
        ├→ Task: "Groceries"
        └→ Task: "Clothes"
```

---

## 🎯 Request/Response Cycle

```
CLIENT (Flutter App)           SUPABASE                 DATABASE
                               (Cloud)                  (PostgreSQL)

User Action
   │
   ├─→ [API Request]
   │   ┌──────────────────┐
   │   │ POST /rest/v1/   │
   │   │ tasks            │
   │   │ {                │
   │   │  title: "...",   │
   │   │  priority: "...", 
   │   │  user_id: auth.id│
   │   │ }                │
   │   └────────┬─────────┘
   │            │
   │            ├─→ [Validate JWT]
   │            │   Get user_id from token
   │            │
   │            ├─→ [Check RLS]
   │            │   Ensure user_id matches
   │            │
   │            ├─→ [INSERT]
   │            │   INSERT INTO tasks
   │            │   WHERE user_id = validated
   │            │
   │            │   ┌───────────────┐
   │            │   │ PostgreSQL    │
   │            │   │ - Validates   │
   │            │   │ - Inserts row │
   │            │   │ - Returns ID  │
   │            │   └───────────────┘
   │            │
   │   ┌────────┴──────────┐
   │   │ [API Response]    │
   │   │ {                 │
   │   │  id: "uuid123",   │
   │   │  title: "...",    │
   │   │  created_at: now  │
   │   │ }                 │
   │   └────────┬──────────┘
   │
   └─← [Receive & Update UI]
       Add task to list
       Show success message
```

---

## 📱 Complete User Journey

```
Day 1 - First Time User:
┌────────────────┐
│ 1. Opens App   │──→ LoginScreen shown
│    First Time  │
└────────────────┘
         │
         ↓
┌──────────────────────────┐
│ 2. Clicks "Sign in w/    │
│    Google"               │
└────────┬─────────────────┘
         │
         ↓
┌──────────────────────────┐
│ 3. Completes Google      │
│    OAuth                 │
└────────┬─────────────────┘
         │
         ↓
┌──────────────────────────┐
│ 4. User saved to DB      │
│    Redirected to Home    │
└────────┬─────────────────┘
         │
         ↓
┌──────────────────────────┐
│ 5. Creates first task    │
│    "Buy groceries"       │
└────────┬─────────────────┘
         │
         ↓
┌──────────────────────────┐
│ 6. Task saved to DB      │
│    Shows in list         │
└────────────────────────────┘

Day 2 - Returning User:
┌────────────────┐
│ 1. Opens App   │──→ Checks auth state
│    Second Time │
└────────┬───────┘
         │
         ├─→ User still logged in?
         │   YES
         │
         ↓
┌──────────────────────────┐
│ 2. Home Screen shown     │
│    with previous tasks   │
│    from Day 1            │
└────────┬─────────────────┘
         │
         ↓
┌──────────────────────────┐
│ 3. Can continue using    │
│    app normally          │
│    All data synced!      │
└────────────────────────────┘

Logout Flow:
┌────────────────┐
│ User in        │
│ Settings → Taps│
│ "Logout"       │
└────────┬───────┘
         │
         ↓
┌────────────────────────┐
│ SupabaseService        │
│ .signOut()             │
│ Clears session         │
└────────┬───────────────┘
         │
         ↓
┌────────────────────────┐
│ Redirected to          │
│ LoginScreen            │
│ Can sign in again ✅   │
└────────────────────────┘
```

---

*These diagrams show the complete architecture and data flow of your MyTask app!*
